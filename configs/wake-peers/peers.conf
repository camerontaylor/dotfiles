# Peer hosts for wake-peers fanout.
# One short hostname per line. Lines starting with # are ignored.
# Self (the current host's `scutil --get LocalHostName` or `hostname -s`) is
# filtered out at runtime by scripts/wake-peers.
neptune
saturn
eris
