emulate -L zsh

local root branch
root="$(_webfront_root)" || {
    print -u2 -- "p is only available inside a compatible webfront repository"
    return 1
}

branch="$(command bash "$root/scripts/wt-plan.sh" "$@")" || return
wtp cd "$branch"

# vim: ft=zsh
